# 11 — Security, Privacy, Permissions, and Release Readiness Ledger

Scope: auth/token storage and Keychain use, sensitive logging, account deletion cleanup, privacy policy links, privacy manifest vs. APIs actually used, associated domains, App Groups, Siri entitlement, background modes, microphone description necessity, notification permission flow, iCloud capability, Share Extension data boundary, universal-link/URL-scheme input validation, release signing configuration, hard-coded dev endpoints/secrets, crash/assert/debug behavior in release, and bundle identifier/version/build/deployment-target continuity.

Status vocabulary: PASS, PARTIAL, MISSING, REGRESSION, MANUAL VERIFY, INTENTIONALLY DEFERRED, NOT APPLICABLE, UNKNOWN / INSUFFICIENT EVIDENCE.

| ID | Area | Legacy behavior/declaration | Legacy evidence | Native evidence | Status | Severity | Notes |
|---|---|---|---|---|---|---|---|
| SEC-001 | Auth token storage | `expo-secure-store` (Keychain-backed) | `AuthContext.tsx` | Raw Security-framework Keychain calls, same backing store | PASS | — | Equivalent mechanism. Cross-reference DATA-033: incompatible service/account identifiers mean no session migrates, but the storage mechanism itself is sound |
| SEC-002 | Sensitive data in logs | No structured logging | N/A | `AppLog.swift` — 5 `os.Logger` categories; no token/password/session-content values logged in any reviewed call site | PASS | — | Net-new addition, clean |
| SEC-003 | Account deletion — local+cloud cleanup | Server-side delete only; explicit UI copy states local downloads/cache/settings are NOT affected | `app/delete-account.tsx:41-46` | Deletes account, signs out (clears Keychain/cookies/Spotlight), does NOT call `clearAllLocalData()` — matches legacy's documented "local data stays until you clear it separately" design | PASS | — | Native goes further: also deindexes Spotlight and purges session cookies |
| SEC-004 | Privacy manifest (PrivacyInfo.xcprivacy) | UserDefaults/FileTimestamp/DiskSpace/SystemBootTime reason codes, RN-oriented | `assets/PrivacyInfo.xcprivacy` | Rewritten from scratch for Swift; declares UserDefaults (CA92.1), FileTimestamp (0A2A.1), DiskSpace (E174.1); correctly omits SystemBootTime (an RN/AsyncStorage-internal reason, not applicable to native) | PASS | — | More accurate than blindly carrying forward legacy's manifest — but reason codes were not independently re-verified against Apple's currently published list before submission |
| SEC-005 | Associated Domains entitlement | `associatedDomains: [...]` declared | `app.config.ts:30-33` | No `com.apple.developer.associated-domains` capability found in project.pbxproj or any entitlements file (full-repo grep, zero hits) | MISSING | High | Duplicate of NATIVE-019 / LIFECYCLE-014 — filed here too since it's also an App Store capability-declaration gap. Universal links from applevis.com cannot open the app at all until this is added |
| SEC-006 | App Groups actual usage | `group.com.applevis.app` shared across multiple features | `app.config.ts` | Used by AppShareConsumer, FocusFilterIntent, ShareExtension — all in-scope/retained; Widget/LiveActivity consumers correctly absent | PASS | — | App Group is genuinely still needed by retained features and is correctly not removable despite Widget/LiveActivity deferral |
| SEC-007 | Siri entitlement/usage-description declaration | Declared alongside 7 intents | `app.config.ts:36,44` | Absent (see NATIVE-007) | PARTIAL | Low–Medium | Cross-reference NATIVE-007 — likely a non-issue for AppIntents-only usage, but confirm intentional |
| SEC-008 | Background modes declared vs. used | `[audio, remote-notification, fetch]` | `app.config.ts` | `[audio, processing]` — no `remote-notification` | PARTIAL | Medium | A silent/`content-available` push from Drupal would not wake native in the background. Every mode native DOES declare is genuinely used (no over-provisioning found). Cross-reference NATIVE-013 and API-026 |
| SEC-009 | Microphone usage description | `NSMicrophoneUsageDescription` declared defensively ("does not record audio") | `app.config.ts:40-41` | Absent from native Info.plist; zero recording-category API usage found (only `.playback` AVAudioSession category) | PASS (correct omission) | — | Improvement — avoids an unneeded permission prompt since native never touches record/mic APIs |
| SEC-010 | Universal-link input validation | N/A | — | `handleUniversalLink` explicitly rejects spoofed hosts via `hasSuffix`, not a naive `.contains()` (own comment calls this out) | PASS | — | Good defensive coding; currently unreachable in practice per SEC-005 until the entitlement is added, but correct once it is |
| SEC-011 | Custom-scheme input handling | Share Extension/Siri intents construct these themselves (not attacker-controlled) | N/A | Fixed set of hosts, defensive query parsing via `URLComponents`, unmapped host = no-op | PASS | — | No injection surface found |
| SEC-012 | Release signing / aps-environment | Managed per-profile by EAS Build | `app.config.ts` (implicit) | Single entitlements file used for BOTH Debug and Release, hardcodes `aps-environment = development` | PARTIAL | Medium | If automatic signing doesn't rewrite this at archive/export time, a TestFlight/App Store build could ship with the dev APNs environment, breaking push for all production users — needs a pre-submission check against an actual exported .ipa, not an assumption |
| SEC-013 | Hard-coded dev endpoints/secrets | N/A | — | Full-tree grep for localhost/ngrok/api_key/secret/password patterns: zero real matches (only false positives) | PASS | — | Clean |
| SEC-014 | Crash/debug behavior shipping in Release | N/A | — | Zero `fatalError(`; 2 `assertionFailure(` in non-user-reachable guards (no-ops in Release); 1 `print(` gated behind `#if DEBUG` | PASS | — | Clean |
| SEC-015 | Bundle identifier + version/build/deployment-target continuity | Production: `com.applevis.app`, version 2026.0.7, build 11, iOS 16.0 minimum | `app.config.ts` | `com.applevis.AppleVisSwift`, version 1.0, build 1, `IPHONEOS_DEPLOYMENT_TARGET = 26.5` (no iOS 26.5 exists as of this audit — this value is almost certainly a placeholder/typo) | MISSING / REGRESSION (release-readiness) | High | Confirms and extends the prior static review's findings. Wrong bundle ID means the app cannot replace the existing App Store listing (existing users would receive a separate app, not an update); the version/build would be rejected as a downgrade; the deployment-target value appears to be a typo, not a deliberate support-policy decision. Needs a deliberate product decision, not a code fix, before any submission |

## Status totals for this ledger

PASS: 9 · PARTIAL: 3 · MISSING: 2 (one shared with NATIVE-019) · REGRESSION: 1 · MANUAL VERIFY: 0 (15 rows)

## Highest-priority items

1. **SEC-015 (High)** — bundle ID / version / build / deployment-target all need explicit reconciliation before any TestFlight or App Store submission; this blocks release regardless of feature completeness.
2. **SEC-005 (High, = NATIVE-019 = LIFECYCLE-014)** — associated domains entitlement missing; universal links are non-functional in production. Count once in the final tally, not three times.
3. **SEC-012 (Medium)** — verify the exported archive actually carries `aps-environment = production`, not the checked-in dev value.
4. **SEC-008 (Medium)** — decide whether `remote-notification` background mode should be restored, contingent on resolving the push-token-format question in API-026.
