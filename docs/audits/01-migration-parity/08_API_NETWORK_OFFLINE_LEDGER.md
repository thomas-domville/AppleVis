# 08 — API, Networking, Cache, Offline, and Failure-Injection Ledger

Scope: every legacy service/API call mapped to native networking — endpoints, methods, auth, headers, query params, pagination, parsing, transforms, date handling, error mapping; plus API health, caching, background fetch, retries/timeouts/cancellation, token expiry, offline banners, and failure-state handling.

Status vocabulary: PASS, PARTIAL, MISSING, REGRESSION, MANUAL VERIFY, INTENTIONALLY DEFERRED, NOT APPLICABLE, UNKNOWN / INSUFFICIENT EVIDENCE.

| ID | Endpoint/Area | Legacy behavior | Legacy evidence | Native evidence | Status | Severity | User impact | Accessibility impact | Manual test needed | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| API-001 | Forums list ("Recent"/filters) | GET `/api/v1/forums/recent?page=N[&apple_only[]=tid...]`, flat REST, 20/page, sorted by last-comment | `src/services/api.ts:741-761` (RECENT_PAGE_SIZE=20) | `Endpoints/ForumEndpoints.swift:17-27` `client.get("forums/recent", queryItems:)` | PASS | Low | None | None | No | Same path, params, page size |
| API-002 | Forums list ("New" filter) | JSON:API `/node/forum?sort=-created&page[limit]=20&include=uid,taxonomy_forums` + optional `filter[since]`/apple-only NOT-IN filter | `api.ts:764-786` | No dedicated "New" JSON:API sort path found in `ForumEndpoints.swift` — only `recent()` | PARTIAL | Medium | "New" sort-by-creation forum filter may be missing/degraded natively | None | Yes — check Forums screen filter picker for a "New" option and confirm its query | Confirm whether native folds "New" into `recent()` or drops it |
| API-003 | Forum categories | GET `/jsonapi/taxonomy_term/forums?fields=name,drupal_internal__tid&sort=name` | `api.ts:814-828` | `ForumEndpoints.swift:29-40` — same path/fields | PASS | Low | None | None | No | topicCount hardcoded 0 both sides implicitly |
| API-004 | Forum topic detail + replies | GET `/node/forum/{id}?include=uid,taxonomy_forums` + `/comment/comment_forum?filter[entity_id.id]=&sort=created&page[limit]=100`, parallel via `Promise.all` | `api.ts:886-917` | `ForumEndpoints.swift:49-95`, parallel via `async let`, same page[limit]=100 | PASS | Low | None | None | No | Native additionally remaps 400→notFound — an improvement, not a regression |
| API-005 | Forum topic/reply submit | POST `/node/forum`, POST `/comment/comment_forum` | `api.ts:930-966` | `ForumEndpoints.swift:101-131` | PASS | Low | None | None | Yes — submit a reply, confirm appears immediately | |
| API-006 | Follow/unfollow (forums, generic) | POST `/flagging/subscribe_node`; unfollow = list then DELETE by flagging id | `api.ts:698-737, 971-1001` | `ContentEndpoints.swift:381-399` `FlagEndpoints` | PASS | Low | None | None | No | Identical two-step unfollow pattern |
| API-007 | Podcast episodes list/detail | JSON:API `/node/podcast?sort=&include=field_podcast,uid,taxonomy_vocabulary_15&page[limit]=20[&filter tag]` | `api.ts:1005-1043` | `PodcastEndpoints.swift:12-47` | PASS | Low | None | None | No | Sort enum matches |
| API-008 | Podcast tag vocabulary | GET `/api/v1/podcast/categories` (REST) | `api.ts:1024-1035` | `PodcastEndpoints.swift:28-35` | PASS | Low | None | None | No | |
| API-009 | Podcast comments + submit + pagination | `/comment/comment_node_podcast` list/submit/moreComments(offset) | `api.ts:1046-1075` | `PodcastEndpoints.swift:50-89` | PASS | Low | None | None | No | |
| API-010 | Podcast transcript | GET `/api/v1/podcasts/episodes/{id}/transcript` returning `{text, vttUrl}` | `api.ts:1120-1121` | `PodcastEndpoints.swift:91-95` — only decodes `text`, drops `vttUrl` | PARTIAL | Low | VTT-based transcript sync/highlighting (if any) unavailable natively | None directly | Yes — check if any native UI expects a VTT URL for transcript timing | Low-severity field drop |
| API-011 | Podcast "love a comment" flag | POST/DELETE `/flagging/love_it` (legacy itself marks it unconfirmed/TODO) | `api.ts:1077-1118` | No `love_it` flag endpoint found natively | MISSING | Low | "Love" reaction on podcast comments unavailable natively | None | No | Low-value gap, legacy itself unconfirmed |
| API-012 | App directory list/detail/reviews | JSON:API `/node/ios_app_directory` + comments, parallel detail+reviews fetch | `api.ts:1185-1194, 1301-1398` | `AppEndpoints.swift:33-138` | PASS | Low | None | None | No | |
| API-013 | App category browse (native REST) | GET `/api/v1/apps/{platform}/categories[/{id}]` | `api.ts:1125-1183` | `AppEndpoints.swift:13-69` | PASS | Low | None | None | No | |
| API-014 | App category browse (JSON:API relationship-probe fallback) | Iterates candidate fields as an "experiment" fallback | `api.ts:1196-1250` | Not found | MISSING | Low | Low practical impact — REST path (API-013) is primary/confirmed-working | None | No | Legacy calls this an experiment; reasonable drop |
| API-015 | App public-HTML scraping fallback | `publicCategory()`/`parsePublicAppDirectory()`/`publicSite()` | `api.ts:475-552, 1252-1293, 1698-1703` | Not found | MISSING | Low | Loses last-resort source if REST+JSON:API both fail | None | No | Intentional simplification |
| API-016 | App submit new listing | POST `/node/ios_app_directory`, fixed vocabulary_1 UUID map | `api.ts:1403-1514` | `AppEndpoints.swift:160-229` — identical UUID table | PASS | Low | None | None | Yes — submit a test app entry | |
| API-017 | Resources/Guides list/detail/comments | JSON:API `/node/guides` + comments | `api.ts:1517-1598` | `ContentEndpoints.swift:15-104` | PASS | Low | None | None | No | |
| API-018 | Blogs list/detail/comments | JSON:API `/node/blog2` + comments | `api.ts:1604-1666` | `ContentEndpoints.swift:109-190` — same `blog2` constant | PASS | Low | None | None | No | |
| API-019 | Bug reports list/detail | JSON:API `/node/ios_bug_report`\|`/node/os_x_bug_report` | `api.ts:2138-2161` | `ContentEndpoints.swift:195-259` | PASS | Low | None | None | No | |
| API-020 | Bug report comments | Legacy is READ-ONLY (no submit function) | `api.ts` bugs block has no submit fn | `ContentEndpoints.swift:261-286` HAS `submitComment` for bugs | REGRESSION (scope drift, not a loss) | Low | Native lets users post bug-report comments legacy couldn't — a feature addition, but a drift from the parity baseline that needs sign-off | None | Yes — confirm Drupal actually accepts posts to this comment bundle | Flag for product sign-off: intentional improvement or scope creep? |
| API-021 | Search (per-content-type CONTAINS) | 5 separate CONTAINS calls, doesn't cover podcasts/bugs | `api.ts:1670-1703` | `ContentEndpoints.swift:293-358` `SearchEndpoints.query` covers forums/apps/guides/blogs/podcasts/iOS+macOS bugs — broader | PASS (native broader) | Low | Native search covers more content types | None | No | Native surfaces `failedCategories`; legacy silently swallows per-type errors |
| API-022 | Full-text search (Search API) | Gated behind `SEARCH_API_PATH`, falls back to CONTAINS when unset | `api.ts:1705-1727` | No `fullText`/gated path found natively | INTENTIONALLY DEFERRED | Low | Native always uses per-type CONTAINS | None | No | Legacy itself treats this as an unconfirmed "Phase 4 gate," never fully turned on |
| API-023 | Public HTML site search fallback | GET `/search?key=` scraped | `api.ts:1698-1703` | Not found | MISSING | Low | No fallback search path | None | No | Same class of gap as API-015 |
| API-024 | Sign-in | POST `/user/login`; **retries once on 403** with no-cache + credentials:omit | `api.ts:1738-1797` | `AccountEndpoints.swift:11-46` — single POST, **no 403 retry** | PARTIAL | Medium | Users hitting a transient 403 on first login (a documented real-world case legacy fixed) get a hard failure natively instead of silent recovery | Failed sign-in forces manual VoiceOver retry, extra friction | Yes — reproduce a 403 on login and compare recovery | This retry was deliberately added to legacy to fix an observed bug; native dropped it |
| API-025 | Sign-out | POST `/user/logout` | `api.ts:1799-1803` | `AccountEndpoints.swift:48-56` | PASS | Low | None | None | No | Both best-effort/non-fatal |
| API-026 | Push token registration | Resolves UUID then PATCHes `field_push_token`/`field_push_sound` via **Expo push token** | `services/notifications.ts:100-132`, `api.ts:1805-1815` | `NotificationEndpoints.swift:26-35` PATCHes same fields but with a **raw APNs device token**, not an Expo token | REGRESSION | High | Native's own code comment states pushes registered this way "won't receive anything" unless Drupal's send logic is changed to call APNs directly — pushes are likely silently non-functional | Users relying on push notifications get none, with no error surfaced | Yes — send a real test push to a native-registered device and confirm delivery | `NotificationEndpoints.swift:14-22` comment explicitly documents this as an open risk — must be resolved with the backend owner before ship |
| API-027 | Account profile fetch/edit/roles/delete | via `/user/user/{uuid}` GET/PATCH/DELETE | `api.ts:1874-1926, 1837-1857` | `AccountEndpoints.swift:60-115`, `ContentActionEndpoints.swift` | PASS | Low | None | None | No | Native adds `fetchProfileFields` (pre-fills Edit Profile) — an improvement |
| API-028 | Contact another user | POST `/contact_message` with session-token CSRF | `api.ts:2108-2131` | `ContentActionEndpoints.swift:104-127` | PASS | Low | None | None | No | Both best-effort/silent-swallow on server rejection |
| API-029 | Content edit/delete/unpublish | PATCH/DELETE across comments/nodes | `api.ts:1930-2076` | `ContentActionEndpoints.swift:10-65` — 1:1 mapping | PASS | Low | None | None | Yes — edit/delete a comment and forum post as admin | |
| API-030 | Drupal webform submissions (blog/bug/podcast/contact) | Scrape form_build_id/form_token/captcha, POST, detect success via redirect-URL change | `services/drupalForm.ts` | `DrupalFormClient.swift` — same token regexes, same redirect-detection, same field names | PASS | Low | None | None | Yes — submit each of the 4 webforms end-to-end | Native's own comments note blog/bug submission "could not be end-to-end verified against a live authenticated session" |
| API-031 | iTunes metadata lookup | Includes `languages`, `ipadScreenshotUrls`, `developerWebsite` | `itunesApi.ts:72-123` | `ItunesAPI.swift:77-125` — omits those 3 fields | PARTIAL | Low | iPad screenshots/language list/developer website link unavailable if UI ever surfaces them | None | Yes — confirm App Detail doesn't have dead UI expecting these | Also: native can't distinguish "not found" from "network error" (both return nil); legacy can |
| API-032 | iTunes search / developer's other apps | — | `itunesApi.ts:140-209` | `ItunesAPI.swift:44-73, 130-152` | PASS | Low | None | None | No | |
| API-033 | API health monitor / circuit breaker | Per-group up/down/unknown, 60s recovery, **actively probed at app launch** | `apiHealth.ts`; wired at `app/_layout.tsx:337` | `ApiHealthMonitor.swift` — same 60s recovery/states, but **no probe()/reset()/isFullyOffline(), nothing calls a proactive probe at launch** | PARTIAL | Medium | Native only learns a group is down reactively after a failed fetch — no pre-warmed health state at cold launch | Slightly slower/less predictive stale-content banner on cold launch offline | No | Threshold/backoff logic matches; only proactive-probe-on-launch is missing |
| API-034 | Cached API / content cache (TTL, staleness) | AsyncStorage TTL table, 5min forum-detail → 30-day podcasts/apps/resources/blogs | `contentCache.ts` | `ContentCache.swift` — disk-backed, **identical TTL table**, plus explicit bugs TTL entries legacy lacks | PASS | Low | None | None | No | Native arguably more correct for bugs caching |
| API-035 | Cache warm-up (prefetchAll) | On launch, fires first page of all 6 content groups in parallel, gated on fast connection | `cachedApi.ts:33-46,180-196`; wired at `app/_layout.tsx:338` | No equivalent prefetch-on-launch found anywhere native | MISSING | Medium | Native tabs load empty/spinner on first visit even on fast wifi where legacy would already have warm cache | Extra VoiceOver loading announcements per tab instead of instant content | Yes — cold-launch on wifi, immediately visit each tab, compare time-to-content | |
| API-036 | Background feed-refresh task | Two overlapping ~15min `expo-background-fetch` tasks: feed refresh + `prefetchAll()` | `services/backgroundFetch.ts`, `tasks/backgroundFetch.ts`; both registered at `app/_layout.tsx:134-135` | Full-tree grep for `BGTaskScheduler`/`BGAppRefreshTask`/`BGProcessingTask` finds **only one registration**, scoped solely to podcast downloads (`Services/BackgroundDownloadTask.swift:14`) | MISSING | High | Native has NO general content/feed background-refresh task at all — content only updates when foregrounded + manually refreshed | Users expecting overnight forum/podcast activity already loaded get stale data silently | Yes — background the app 20+ min with new content posted server-side, foreground and check Home without a manual pull-to-refresh | **Single largest confirmed parity gap in this ledger** — corroborates the prior static review's claim with a full-tree grep, not just one file |
| API-037 | Background podcast auto-download task | 30-min interval, honors wifiOnly/always/off, up to 3 new episodes | `services/backgroundFetch.ts:65-99` | `Services/BackgroundDownloadTask.swift` — `BGProcessingTask`, same 30-min/wifiOnly semantics, up to 3 episodes | PASS | Low | None | None | Yes — verify actual download under Wi-Fi-only setting | This is native's ONLY background task, confirming API-036's gap |
| API-038 | Retries / timeouts / cancellation | 10s AbortController timeout on every fetch, no auto-retry except sign-in's 403 | `api.ts:38,67-152`; `apiHealth.ts:15` | `APIClient.swift:96-97` — 10s request/30s resource timeout, no auto-retry | PASS (see API-024 gap) | Low | None | None | No | Native's 30s resource ceiling is a reasonable addition |
| API-039 | Token expiry / session-expired handling | On 401, emits sessionExpiry event → root layout signs out + shows message | `authEvents.ts`; `cachedApi.ts:81-85` | `APIClient.swift:298-305` → `AuthStore.handleSessionExpired()` + `ToastStore.error`; `AuthStore.swift:66-78` clears local state without calling the logout endpoint | PASS | Low | None | Confirm toast is VoiceOver-announced | Yes — force a 401 mid-session, confirm sign-out + toast + no crash | Different mechanism (static ref vs event bus), equivalent effect |
| API-040 | Stale-cache handling + staleness cue | Falls back to last non-expired cache, banner shows "Showing saved content from Xm/h/d ago," announced + assertive live region + sound | `cachedApi.ts:59-98`; `components/OfflineBanner.tsx` | `CachedFetch.swift:11-43` same fallback logic; `Views/Shared/OfflineBanner.swift` — static text, **no cache-age display**, wording says "You're offline" even when the cause is a degraded API group, not true offline | PARTIAL | Low | Native banner doesn't tell the user how old cached content is; wording can mislead about true cause | VoiceOver users get less precise staleness info | No | Both fire sound + announcement on first appearance — parity there; wording/age-display gap is polish, not a functional break |
| API-041 | Offline banner trigger conditions | Fires whenever cache was served (`fromCache`) | `cachedApi.ts:48-58`; `OfflineBanner.tsx:16-32` | Per-screen: `degradedGroups.contains(.group)` (same trigger class); Home additionally ORs in true `!networkMonitor.isConnected` | PASS | Low | None | None | No | Native's Home-specific extra condition is a reasonable improvement |
| API-042 | Online→offline→online transition | No auto-refresh on reconnect either side; manual pull-to-refresh required | `apiHealth.ts:94-103` doc comment | No `NetworkMonitor`-triggered auto-refetch found | PASS (parity — both manual) | Low | Neither app auto-refreshes on reconnect | None | Yes — go offline on a stale screen, reconnect without interacting, confirm neither auto-updates | Shared limitation, not a regression |
| API-043 | Malformed/missing-field defensive parsing | `??`/optional-chaining fallbacks everywhere; `normaliseTimestamp` handles numeric-string/unix-s/unix-ms/ISO/invalid | `api.ts` mappers throughout; `normaliseTimestamp:422-435` | `Mappers.swift` same fallback pattern; `APIClient.swift:111-127` custom date strategy mirrors the same cascade | PASS | Low | None | None | No | Close 1:1 port |
| API-044 | Server 4xx/5xx handling — visible text + retry | `{ok:false, error, status}`, no confirmed shared ErrorView convention | `api.ts:66-98` | Shared `ErrorView` (`Views/Shared/LoadingView.swift:33-48`) with typed `APIError` enum, retry button, used consistently | PASS (native more consistent) | Low | None | None | No | Native gives more specific error text (timeout/unauthorized/forbidden/rateLimited/server/notFound) |
| API-045 | Partial-result-source handling (Home feed) | No confirmed `Promise.allSettled` usage; each cachedApi call independently falls back | grep found no matches in `app/` | `HomeViewModel.swift:36-78` — explicit `failedSourceNames`, keeps existing items on total failure rather than blanking | PASS (native more explicit) | Low | None | Confirm `failedSourceNames` is actually VoiceOver-announced | Yes — force one content-group API to fail while others succeed, confirm Home still renders + communicates partial failure | |
| API-046 | Duplicate-content deduping | No explicit dedup step found in Home/feed assembly | — | `HomeViewModel.swift:89-101` — explicit `deduplicated()` by `FeedItem.id`, comment explains it fixes a real SwiftUI-identity-collision/pagination-overlap bug (duplicate ids → wrong-item taps → 400s) | PASS (native adds a fix) | Low | None | None | No | Worth flagging upstream to legacy too, though out of scope here |
| API-047 | Image/audio URL failure fallback | No explicit onError/fallback found | — | `AsyncImage` used throughout with built-in phase handling, but no explicit fallback-image asset confirmed at each call site | UNKNOWN / INSUFFICIENT EVIDENCE | Low | Broken artwork/icon URLs may show blank space rather than a fallback glyph, either platform | Depends on whether failed-image state has an accessible label | Yes — force an artwork/icon URL to 404, compare visual/VoiceOver fallback both apps | Flagged for follow-up in the visual/UI-layer ledger |
| API-048 | Submission validation + server-rejection surfacing | Redirect-based success signal only; validation failure → one generic string, no field-level detail | `drupalForm.ts:59-62,97-102` | `DrupalFormClient.swift:78-98` — identical redirect-detection heuristic, identical generic failure string | PASS (parity — same limitation) | Low | Neither app surfaces field-specific validation errors for the 3 webform flows | VoiceOver users get no indication of which field was rejected | Yes — submit with a deliberately invalid field, compare error specificity (expect both equally generic) | Shared limitation, not a native regression |
| API-049 | Download interruption / storage-full handling (network-layer touchpoints only) | Not covered by this lens's file set | — | `Services/BackgroundDownloadTask.swift:39-56` — cancellable `Task` with proper `expirationHandler` for time, but no confirmed disk-space handling | MANUAL VERIFY | Medium | Background auto-download could fail silently on storage-full | None directly | Yes — fill device storage near-full, trigger background auto-download, confirm graceful failure | Cross-reference with the podcast/audio ledger's downloads section |

## Status totals for this ledger

PASS: 31 · PARTIAL: 6 (API-002, API-010, API-024, API-031, API-033, API-040) · MISSING: 6 (API-011, API-014, API-015, API-023, API-035, API-036) · REGRESSION: 2 (API-026 negative, API-020 positive/scope-creep flag) · MANUAL VERIFY: 1 (API-049) · UNKNOWN / INSUFFICIENT EVIDENCE: 1 (API-047) · INTENTIONALLY DEFERRED: 1 (API-022) · NOT APPLICABLE: 0 (49 rows)

## Highest-priority findings

1. **API-036 (High)** — no general background feed-refresh task exists natively; only podcast auto-download runs in the background, confirmed via full-tree grep for `BGTaskScheduler`/`BGAppRefreshTask`/`BGProcessingTask`.
2. **API-026 (High)** — native registers a raw APNs token against a Drupal field the legacy app populated with an Expo push token; native's own code comments flag push delivery as likely broken.
3. **API-035 (Medium)** — no cache warm-up/prefetch-on-launch equivalent to legacy's `apiHealth.probe()` + `cachedApi.prefetchAll()`.
4. **API-024 (Medium)** — native's sign-in lacks legacy's documented 403-retry-with-no-cache fallback.
